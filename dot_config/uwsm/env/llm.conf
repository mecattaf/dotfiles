# Environment variables for LLM services
# These will be available to all services in the session

# Display for potential future GUI tools
WAYLAND_DISPLAY=${WAYLAND_DISPLAY}
DISPLAY=${DISPLAY}

# XDG Runtime
XDG_RUNTIME_DIR=${XDG_RUNTIME_DIR}

# Container runtime preferences
CONTAINER_HOST=unix:///run/user/${UID}/podman/podman.sock
