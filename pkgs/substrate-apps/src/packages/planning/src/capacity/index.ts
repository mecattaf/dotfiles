/**
 * Seat capacity: projection, staleness and admission (pure), and the ledger
 * that keeps the latest reading per seat in the object's store.
 */
export * from "./project.ts";
export * from "./ledger.ts";
