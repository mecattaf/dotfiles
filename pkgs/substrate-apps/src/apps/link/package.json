{
  "name": "@substrate/link",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "The outbound link on the hypervisor host: leases AgentJobs from the floor over /rpc (Lease, Heartbeat, Complete) and runs each as one ax Task (create-only, durable verdict outbox).",
  "license": "UNLICENSED",
  "exports": { ".": "./src/link.ts", "./*": "./src/*" },
  "scripts": { "check": "tsc --noEmit -p .", "test": "vitest run" },
  "dependencies": {
    "@grpc/grpc-js": "^1.12.6",
    "@grpc/proto-loader": "^0.7.13"
  },
  "peerDependencies": { "effect": "4.0.0-rc.112" },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "effect": "4.0.0-rc.112",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
