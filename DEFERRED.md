# DEFERRED

What a captured ruling bars this repository's units from doing, and who does it
instead. A row leaves when the act has been taken, not when it has been argued
about.

| id | deferred | why it is barred here | who takes it, and when |
|---|---|---|---|
| DF-U-D16-2 | The WORKER box's switch (the coordinator's is taken) | U-D16's non-goals: "no switch here; no llama-swap restart". The coordinator half is DONE — U-D19 took it, generation 190, `nixos-rebuild switch --flake .#coordinator` rc 0, llama-swap's `ActiveEnterTimestamp`, `MainPID` and `NRestarts` all unchanged against `tools/u-d19-switch-baseline.env`. The worker box's switch stays a TOM LINE by the same ruling (D-B15) | Tom, on the worker. Tracked once, for every unit that names it, as `DF-U-D19-4` |
| DF-U-D12-2 | Giving `pi-qwencloud` a measured allowance/window or releasing an item onto it | D-B17 keeps TL-17 unset: the row stays UNKNOWN, is never proposed onto, and items naming it remain `unclaimed` carrying this deferral | Tom supplies one of TL-17's named settling artefacts; the later row writer may then replace UNKNOWN with that measured statement |
| DF-U-D12-3 | Refreshing the expired `cc3` OAuth token | D-B5 records this as the TOM LINE "run `claude` once on cc3". This feeder may read the credential through `stamp-receipt.py window`; it may not perform an interactive login | Tom runs `claude` once with the cc3 seat configuration. Until then the enabled feeder writes a fresh UNKNOWN row with the reader's reason |
| DF-U-D12-4 | Releasing work onto the `codex` row | D-B6 says the login is third-party and the row is never proposed onto. Reading `rate_limits` from its rollout is not a spend and does not change ownership | A later explicit ownership ruling (TL-6); this unit preserves `owner: third-party` in every write |
| DF-U-D17-1 | Turning `l8-flash-probe`'s two UTIL-01 rows green: `util-sampler.timer` on BOTH boxes and `util-row.timer` on the coordinator | Two things now stand between this row and its discharge, and neither is U-D17's. (1) The WORKER half is a TOM LINE (D-B15, `DF-U-D19-4`). (2) The probe cannot report green on the coordinator either, because its `FragmentPath` rows compare the symlink systemd prints rather than what it resolves to — MEASURED 2026-09-07 on generation 190, both timers installed and store-backed, both rows still FAIL, alongside `claude-transcript-mirror.timer` and `nightly-record.timer` for the same reason ([dotfiles#331](https://github.com/mecattaf/dotfiles/issues/331), `DF-U-D19-3`) | Tom for the worker; dotfiles#331 for the probe. `bash tools/u-d17-util-01-oracle.sh` is green today (12/12), and both units ARE installed on the coordinator: `systemctl --user show -p FragmentPath` resolves under `/nix/store` for each |
| DF-U-D17-2 | Starting any serve on this estate with `--metrics`, so UTIL-01's `tokens_in` / `tokens_out` stop being `UNKNOWN` | U-D17's non-goal is "no change to the sampler's semantics", and R-04 forbids reading an unresolved value as zero — so the sampler reports `UNKNOWN` loudly rather than `0`, and the fix is a serve-side change in another issue, not a sampler-side one here. 0 hits for `--metrics` in `lib/local-models.nix` and `modules/llama-swap.nix` | [dotfiles#312](https://github.com/mecattaf/dotfiles/issues/312). Until then "how many tokens per week" stays a question, and `tokens_grade` stays `UNKNOWN` |
| DF-U-D15-1 | Putting the new `herdr-kitten` pin on the WORKER box (the coordinator is on it) | U-D15 declares an input; only a switch installs it, and D-B15 splits the two switches. The coordinator half is DONE — MEASURED 2026-09-07 after U-D19's switch: `readlink -f "$(command -v hk)"` → `/nix/store/wjbbi7n73h2xn5g3nrpfkprr3c2hy28c-herdr-kitten-0.1.0-dev/share/hk/bin/hk`, which is exactly this lock's package (the pre-switch reading was `caj91n9…`, the old generation's). The worker is still on the old pin, which is where the `github:` form actually bites and why the topology must not be moved by a hand-run switch on one box | Tom, on the worker (`DF-U-D19-4`). Discharged when `readlink -f "$(command -v hk)"` there resolves under the same store path `bash tests/herdr/test-herdr-kitten-input.sh` clause C2 prints |
| DF-U-D15-2 | Moving the herdr topology: a second server, or the server off the coordinator | Ruling B5 — ONE herdr server, on the coordinator — and `mecattaf/dotfiles#309` is the open TOM LINE on that question. U-D15's non-goals name it verbatim: "no herdr topology change (#309 stays a TOM LINE: B5 one server on the coordinator)". The `home-profiles` check asserts the current shape in both directions (a `herdr` unit on the coordinator, none on the worker, the `hk` client on both) so the pin move cannot drift into a topology move | Tom, on `#309`. Until then the two asserts in `flake.nix`'s `home-profiles` are what keeps the shape |
| DF-U-D19-1 | Making `tally-uplink.service` reach `active` — i.e. completing ONE wake — while `mecattaf/tally` `26d75804` and `mecattaf/tally-ts-sdk` `a233c303` are what this repository pins | A cross-repo contract mismatch that no option in this repository closes, MEASURED 2026-09-07 on generation 190. The uplink's `probe()` is an `admit` with no `taskId` taken over EVERY row of its rows file in file order; the kernel's `admit` runs `consider()` → `stamp_row(row)`, which refuses `exec_recovery_malformed` for a row outside its own `--rows` table; and this estate's kernel is configured — correctly — with only the three `owner: kernel` rows, because `RowWriter::stamp` WRITES `<meters>/<row>.json` with `"owner":"kernel"` and giving it the seat rows would have it overwrite what U-D12's feeders publish, destroying D-B5's two pools. `docs/rows.md`'s first row is `cc`, so the first probe of every wake throws. The repository's only two levers are the kernel's `--rows` (must stay the owned rows) and the uplink's `--rows` (must stay the estate's nine, asserted by `tally-uplink-topology`), and neither is a fix | [mecattaf/tally#50](https://github.com/mecattaf/tally/issues/50) (the kernel should read the meters dir for a row it does not own, and in any case must not classify a missing row spec as a lease-recovery failure) and [mecattaf/tally-ts-sdk#105](https://github.com/mecattaf/tally-ts-sdk/issues/105) (one refused row must not end the wake — the row is written busy with `grade UNKNOWN`, which is that module's own stated contract). Neither `main` carries a fix today (MEASURED: `add5dddb`, `f817f86d`). Discharged when a bumped pin lands and `bash tools/u-d19-switch-oracle.sh` clause C2 reads `[P]` — then delete this row |
| DF-U-D19-2 | `~/.local/state/tally-rewrite/ledger.jsonl` growing across an UPLINK wake | The chain grows on every answered `admit`: the file exists and carries `admission_transition` records, written by probes over the socket on the rows the kernel owns (MEASURED 2026-09-07). What cannot happen yet is the estate's own traffic, because the wake that would produce it is DF-U-D19-1's. Manufacturing rows by hand to turn the clause green would be the oracle writing the evidence it then reads | The same fix as DF-U-D19-1. Discharged in the same act: clause G1 of `tools/u-d19-switch-oracle.sh` brackets one wake and requires the file to grow across it |
| DF-U-D19-3 | Repairing `home/dot_local/bin/l8-flash-probe`'s `FragmentPath` rows, which compare the symlink systemd prints rather than what it resolves to, and so read FAIL for four units this switch installed correctly | It is U-D16/U-D17's artefact, not this unit's, and `DF-U-D17-1` — the row those two `[E]` rows belong to — cannot be discharged here in any case: its worker-box half is a TOM LINE (`~/research-methods/DECISIONS.md` D-B15). MEASURED 2026-09-07 on generation 190: systemd reports `/etc/systemd/system/<u>` and `~/.config/systemd/user/<u>` for EVERY declared unit on both buses, llama-swap and tally-daemon included, and only `readlink -f` reaches the store | [dotfiles#331](https://github.com/mecattaf/dotfiles/issues/331), with the two-part assertion the rows mean (the `FragmentPath` is a symlink — a plain file there is a hand-installed unit, Rule 9 — and it resolves under `/nix/store`) already written as `frag_ok` in `tools/u-d19-switch-oracle.sh`. A fix must keep accepting a value already under `/nix/store`, since `tests/l8-flash-probe/test-util-timer-rows.sh`'s fake `systemctl` answers those directly |
| DF-U-D19-4 | The WORKER box's switch, `sudo nixos-rebuild switch --flake /home/tom/mecattaf/dotfiles#worker` | `~/research-methods/DECISIONS.md` D-B15: *"the **coordinator** switches under this run, in P05's order, after `nix flake check`; the **worker** box's switch stays a TOM LINE."* The card's non-goals say it a second way — "the worker box untouched" — and the unswitched worker is this unit's NEGATIVE CONTROL (`tools/u-d19-worker-negative-control.sh`), so taking it would destroy the evidence that the coordinator's probes grade the switch and not any box at all | Tom, from the worker's own checkout, after `pull --ff-only` (P05 walkthrough step 6). It is also where `DF-U-D15-1`'s `github:` herdr-kitten pin actually bites, and where `DF-U-D17-1`'s worker half is discharged |
| DF-U-D13-2 | Passing `--evaluator-lock` to the served kernel, so a verdict is derived at all | `services.tally-kernel.evaluatorLock` defaults to null, and null is the honest state: the lock pins `apps/evaluator`'s bytes, which do not exist until U-A17 delivers them in mecattaf/tally-ts-sdk (tally docs/socket.md §4: "Without --evaluator-lock, no verdict is ever derived at all"). Locking a path that does not exist would be a stub standing in for a deliverable | U-A17's delivery, then a follow-up pin of the lock file's path in this module — an edit reviewed like any other, which the topology check will see |
| DF-U-D14-1 | `tally-uplink.service` reaching `active` — the switch that installs it is TAKEN | U-D14's non-goal was "no switch here", and that half is discharged: U-D19 switched the coordinator (generation 190), the unit is installed with `FragmentPath` `/home/tom/.config/systemd/user/tally-uplink.service` → `/nix/store/2i26q9nszxip8000f066bz3icwha1mnd-tally-uplink.service/tally-uplink.service`, the token file exists 0600 (`DF-U-D14-2` discharged, 44 bytes, Tom's act), and `bash tests/tally-uplink/test-tally-uplink-input.sh` is green. What remains is not this repository's: one wake cannot complete at the revs pinned here, because the kernel refuses an `admit` for a row it does not own and the uplink probes all nine — the whole of `DF-U-D19-1` | The upstream fixes `DF-U-D19-1` names ([mecattaf/tally#50](https://github.com/mecattaf/tally/issues/50), [mecattaf/tally-ts-sdk#105](https://github.com/mecattaf/tally-ts-sdk/issues/105)), then a pin bump. Discharged with `DF-U-D19-1`, in the same act — then delete both rows |
| DF-U-D14-2 | ~~Writing `~/.local/state/tally-rewrite/lake-token`~~ — **DISCHARGED**, kept for one release as the record | It was a CREDENTIAL and no executor here reads, prints or writes one; the honest state was a file that did not exist and a unit that fails naming the path it wanted. MEASURED 2026-09-07 before U-D19's switch: the file exists, mode `0600`, 44 bytes, written 02:29 by Tom — out of band from this repository, and never opened by anything in it. No tmpfiles rule names it, then or now | Taken by Tom. Delete this row at the next unit that touches `DEFERRED.md` |
| DF-U-D14-3 | Setting `services.tally-uplink.kit` and `.plan` — the box's argv table and the acceptor's plan body | Both stay null and null is the honest state, not an oversight. NO kit names an argv for this estate: TL-18 is the open Tom line on the Claude-seat one ("no kit names a Claude argv today"), and the uplink never falls back to a command of its own — a proposal carrying an `argv_ref` it cannot resolve is a legible throw, not a guess. The plan body is the ACCEPTOR's, re-POSTed to arm and re-arm; authoring one in this module would be the lake proposing from the wrong side of the seam | TL-18 for the kit (`kits/claude/UNIT.md`, Tom's line or paste), then a follow-up edit of these two options — reviewed like any other change, which the `tally-uplink-topology` check sees because it asserts both are null today |
| DF-U-D14-4 | Giving the uplink a schedule: a timer, an `Install` section, or any `WantedBy` that would fire it | The uplink holds no schedule of its own, by its own card's non-goal ("no scheduling logic in the uplink: the lake proposes, the door answers") and by spec §2.2b — "the uplink wakes on a socket event, on `next_wake_at`, or on a verdict it posted — whichever first — and every wake is a probe plus a pull; there is no interval". `wakes = 1` here is one wake per invocation, never a loop this file authorises, and the rendered unit carries no `Install` section at all (asserted at eval time and by clause E) | U-D18 (DF-FILLER-TIMER): "a systemd user timer that runs the filler lane … the timer only wakes the uplink's filler pass, the kernel admits". Discharged when that unit lands its own timer; nothing in U-D14 shortens the wait |
| DF-U-D18-2 | Setting an anti-starvation number for the filler lane, and implementing D-B10's age-based promotion (a filler item passed over by 10 non-filler releases proposed at level 1 for one release) | Spec §4.4.3 names TL-10 as the anti-starvation number and says that "until it exists the lane runs whenever the row is free". The promotion is a RELEASE-STATION decision over a backlog — the acceptor's `levels` document and the lake's proposals — and a clock has no backlog to age. Putting a promotion rule in a timer would be this unit deciding admission, which is the one thing its own card forbids ("the timer only wakes the uplink's filler pass, the kernel admits") | Tom for TL-10's number; the release station (§2.2c) and the lake's selector for the promotion itself. Nothing in this repository shortens the wait, and the five-minute cadence stands meanwhile: it is the drain's own, so neither filler can crowd the other out |
| DF-U-D18-3 | Moving the academic drain onto a LEASE over the kernel's socket, so the two fillers alternate UNDER the kernel's admission rather than beside it | Spec §2.4 lists the drain as an *unleased* tenant of the GPU row "until TL-15 moves the drain onto a lease over the socket (U-D11 wires the call)". Until that happens, D-B10's round-robin is carried here by equal cadence plus the lane's own `/running`-empty gate — two mechanisms outside the kernel — and this unit cannot lease on another unit's behalf. MEASURED 2026-09-07: `tally-drain.service` runs `tally --socket /run/user/1000/tally/tally.sock daemon drain`, the LIVE daemon's socket, not the rewrite kernel's `~/.local/state/tally-rewrite/kernel.sock` | U-D11 wires the call; TL-15 is the line. Discharged when the drain's GPU turn is a lease on the chain and the `tally-filler-topology` cadence equality can be replaced by an admission-side assertion |

**How DF-U-D16-1 was discharged, recorded because the act was not U-D19's.**
The row named the two hand-written `~/.config/systemd/user/claude-transcript-mirror.*`
files and sequenced their removal inside U-D19. MEASURED 2026-09-07: home-manager
had already taken them over at the 2026-09-06 22:25 switch (generation 188) —
both paths are now symlinks into the store and the originals sit beside them as
`.hm-bak`, dated 2026-09-04 18:13. Nothing in U-D19 deleted anything. Note that
`tools/u-d16-l8-flash-oracle.sh` row `5e` still reports the pair "present",
because it globs `claude-transcript-mirror.*` and so counts the backups and the
symlinks; it is a `NOTE` row and does not gate.

DF-U-D18-1 was DF-U-D13-1's and DF-U-D14-1's twin a third time, and the three
did discharge in one act: U-D19's switch installed the kernel on the system bus
and, on tom's, the uplink and the filler's clock. Two of the three left the
table; DF-U-D14-1 stayed, because installing the uplink and completing a wake
turned out to be two different things (DF-U-D19-1). DF-U-D18-2 and DF-U-D18-3
are of the other kind — they were never waiting on a switch, but on a number Tom
has not named (TL-10) and on a call another unit wires (U-D11/TL-15) — so
neither was discharged by anything U-D19 did, and both stay data.

DF-U-D14-1 was DF-U-D13-1's twin one bus over, and the switch that discharged
one only half-discharged the other: the unit is installed and store-backed, and
it cannot complete a wake at the pinned revs. It is rewritten above around what
actually remains and pairs with DF-U-D19-1. DF-U-D14-2 was the only row in this
table whose actor could not be an executor at all — a credential is Tom's by
construction, and the unit is deliberately built to fail loudly rather than run
without one; Tom wrote the file, and the row is kept struck-through for one
release as the record.

**The four U-D19 rows, and what kind each is.** DF-U-D19-4 is a ruling (D-B15
keeps the worker box's switch a TOM LINE) and is the one every other unit's
"and the worker's" clause now points at. DF-U-D19-3 is a defect in another
unit's artefact that this unit found by switching and is barred from fixing
here (dotfiles#331). DF-U-D19-1 and DF-U-D19-2 are the same blocker twice — one
refused row ends the whole wake — and they are the only two clauses of this
unit's own DOMINANT oracle that do not read green. They are recorded as a
blocker and not softened into a NOTE: the card asks for `active` and the box
says `failed`, and an oracle that graded its author's convenience instead of the
estate would be worth nothing.
