# DEFERRED

What a captured ruling bars this repository's units from doing, and who does it
instead. A row leaves when the act has been taken, not when it has been argued
about.

| id | deferred | why it is barred here | who takes it, and when |
|---|---|---|---|
| DF-U-D16-1 | Removing the two hand-written mirror units: `systemctl --user disable --now claude-transcript-mirror.timer`, then `rm ~/.config/systemd/user/claude-transcript-mirror.service` and `rm ~/.config/systemd/user/claude-transcript-mirror.timer` | `~/sept7/scopes/clean-dotfiles.md:171` §6 — "Tom's shell acts that no executor performs" — names this removal explicitly, alongside `git push origin main`, `gh pr create` and `sudo nixos-rebuild switch`. It is also barred by its own mechanism: until the switch that installs the declared units, the hand-written pair is the ONLY working mirror, so deleting it early stops the mirror and stops it silently | Tom, on the coordinator, as step 4 of the P05 walkthrough (`receipts/L8-FLASH/PR-BODY.md`) — after `/home/tom/mecattaf/dotfiles` carries the branch (done: `e549ba91` is an ancestor of `main`) and before `nixos-rebuild switch`. Sequenced inside U-D19. MEASURED still present 2026-09-06, both files 2026-09-04 18:13 |
| DF-U-D16-2 | `sudo nixos-rebuild switch --flake /home/tom/mecattaf/dotfiles#coordinator`, and the worker's switch | U-D16's non-goals: "no switch here; no llama-swap restart". The coordinator's switch is U-D19's own unit (`~/research-methods/DECISIONS.md` D-B15); the worker box's switch is a TOM LINE by the same ruling | U-D19 for the coordinator; Tom for the worker |
| DF-U-D12-1 | Switching the coordinator generation or starting/stopping any `tally-seat-feeder-*` unit | U-D12's non-goal is "no switch here"; D-B15 assigns the coordinator switch to U-D19. A declaration is the deliverable in this unit, never a hand-installed unit (Rule 9) | U-D19 switches the coordinator in the P05 order; Home Manager then enables the three timers through `timers.target` |
| DF-U-D12-2 | Giving `pi-qwencloud` a measured allowance/window or releasing an item onto it | D-B17 keeps TL-17 unset: the row stays UNKNOWN, is never proposed onto, and items naming it remain `unclaimed` carrying this deferral | Tom supplies one of TL-17's named settling artefacts; the later row writer may then replace UNKNOWN with that measured statement |
| DF-U-D12-3 | Refreshing the expired `cc3` OAuth token | D-B5 records this as the TOM LINE "run `claude` once on cc3". This feeder may read the credential through `stamp-receipt.py window`; it may not perform an interactive login | Tom runs `claude` once with the cc3 seat configuration. Until then the enabled feeder writes a fresh UNKNOWN row with the reader's reason |
| DF-U-D12-4 | Releasing work onto the `codex` row | D-B6 says the login is third-party and the row is never proposed onto. Reading `rate_limits` from its rollout is not a spend and does not change ownership | A later explicit ownership ruling (TL-6); this unit preserves `owner: third-party` in every write |
| DF-U-D17-1 | Turning `l8-flash-probe`'s two new `[E]` rows green: `util-sampler.timer` on BOTH boxes and `util-row.timer` on the coordinator. They ship FAIL | Only a switch installs a declared unit, and this unit switches nothing. `~/research-methods/DECISIONS.md` D-B15 splits the two switches: the coordinator's is U-D19's own unit, the worker box's is a TOM LINE. U-D17's non-goals bar changing which box runs which timer, so nothing here can shorten the wait | U-D19 for the coordinator; Tom for the worker. Discharged when `bash tools/u-d17-util-01-oracle.sh` and `l8-flash-probe` both read green for the two rows on both boxes — then delete this row. MEASURED 2026-09-06: neither unit exists on the coordinator, `FragmentPath` empty for both |
| DF-U-D17-2 | Starting any serve on this estate with `--metrics`, so UTIL-01's `tokens_in` / `tokens_out` stop being `UNKNOWN` | U-D17's non-goal is "no change to the sampler's semantics", and R-04 forbids reading an unresolved value as zero — so the sampler reports `UNKNOWN` loudly rather than `0`, and the fix is a serve-side change in another issue, not a sampler-side one here. 0 hits for `--metrics` in `lib/local-models.nix` and `modules/llama-swap.nix` | [dotfiles#312](https://github.com/mecattaf/dotfiles/issues/312). Until then "how many tokens per week" stays a question, and `tokens_grade` stays `UNKNOWN` |
| DF-U-D15-1 | Putting the new `herdr-kitten` pin ON either box: `sudo nixos-rebuild switch --flake /home/tom/mecattaf/dotfiles#coordinator`, and the worker's switch | U-D15 declares an input; only a switch installs it. `~/research-methods/DECISIONS.md` D-B15 splits the two switches — the coordinator's is U-D19's own unit, the worker box's is a TOM LINE — and this unit's non-goals bar changing the herdr topology, which a hand-run switch on one box would do by accident (one box on `ccc1639`, the other still on `41a6de5`). MEASURED 2026-09-06 on the coordinator: `readlink -f "$(command -v hk)"` → `/nix/store/caj91n9…-herdr-kitten-0.1.0-dev/share/hk/bin/hk`, i.e. the RUNNING generation, while this lock's package is `/nix/store/wjbbi7n…-herdr-kitten-0.1.0-dev` — a different store path, so nothing on either box is on the new pin yet | U-D19 for the coordinator; Tom for the worker. Discharged when `bash tests/herdr/test-herdr-kitten-input.sh` is green in the repo AND `readlink -f "$(command -v hk)"` on each box resolves under the same store path clause C2 prints |
| DF-U-D15-2 | Moving the herdr topology: a second server, or the server off the coordinator | Ruling B5 — ONE herdr server, on the coordinator — and `mecattaf/dotfiles#309` is the open TOM LINE on that question. U-D15's non-goals name it verbatim: "no herdr topology change (#309 stays a TOM LINE: B5 one server on the coordinator)". The `home-profiles` check asserts the current shape in both directions (a `herdr` unit on the coordinator, none on the worker, the `hk` client on both) so the pin move cannot drift into a topology move | Tom, on `#309`. Until then the two asserts in `flake.nix`'s `home-profiles` are what keeps the shape |
| DF-U-D13-1 | Starting `tally-kernel.service` on the coordinator — i.e. the switch that installs the declared unit (`sudo nixos-rebuild switch --flake /home/tom/mecattaf/dotfiles#coordinator`), and anything hand-started in its place | U-D13's non-goal is "no switch (U-D19)"; `~/research-methods/DECISIONS.md` D-B15 assigns the coordinator switch to U-D19 and keeps the worker box's a TOM LINE. A declaration is the deliverable, never a hand-started unit (Rule 9). MEASURED 2026-09-06 on the coordinator: `systemctl status tally-kernel` → "could not be found" — the declared-but-not-switched state, which is the intended one | U-D19 switches the coordinator in the P05 order. Discharged when `systemctl status tally-kernel` is `active` with `FragmentPath` under `/nix/store`, `~/.local/state/tally-rewrite/ledger.jsonl` exists, and `bash tests/tally-b/test-tally-b-input.sh` is green in the repo — then delete this row |
| DF-U-D13-2 | Passing `--evaluator-lock` to the served kernel, so a verdict is derived at all | `services.tally-kernel.evaluatorLock` defaults to null, and null is the honest state: the lock pins `apps/evaluator`'s bytes, which do not exist until U-A17 delivers them in mecattaf/tally-ts-sdk (tally docs/socket.md §4: "Without --evaluator-lock, no verdict is ever derived at all"). Locking a path that does not exist would be a stub standing in for a deliverable | U-A17's delivery, then a follow-up pin of the lock file's path in this module — an edit reviewed like any other, which the topology check will see |
| DF-U-D14-1 | Starting `tally-uplink.service` on the coordinator — i.e. the switch that installs the declared unit (`sudo nixos-rebuild switch --flake /home/tom/mecattaf/dotfiles#coordinator`), and anything hand-started in its place | U-D14's non-goal is "no switch here"; `~/research-methods/DECISIONS.md` D-B15 assigns the coordinator switch to U-D19 and keeps the worker box's a TOM LINE. A declaration is the deliverable, never a hand-started unit (Rule 9). MEASURED 2026-09-07 on the coordinator: `systemctl --user is-active tally-uplink.service` → `inactive` — the declared-but-not-switched state, which is the intended one, and clause F of the test script records it as a NOTE rather than a verdict | U-D19 switches the coordinator in the P05 order, and its own probes name `tally-uplink.service active` among them. Discharged when that unit is `active` with a `FragmentPath` under `/nix/store`, the token file exists (DF-U-D14-2), and `bash tests/tally-uplink/test-tally-uplink-input.sh` is green in the repo — then delete this row |
| DF-U-D14-2 | Writing `~/.local/state/tally-rewrite/lake-token`, the bearer the uplink sends to the deployed Worker | It is a CREDENTIAL, and no executor here reads, prints or writes one. Creating it empty as a placeholder would be worse than absent: an empty bearer is a 401 that reads like a lake outage, so the honest state is a file that does not exist and a unit that fails naming the path it wanted (`cannot read the lake token file <path>`). Deliberately NO tmpfiles rule creates it — clause G asserts that no rule names it, and never opens the file | Tom, on the coordinator, out of band from this repository (the value is the lake's own `LAKE_TOKEN`, banked by the lake repo's deploy receipts). Sequenced before U-D19's switch is worth taking. Discharged when the file exists 0600 and a wake reaches the lake |
| DF-U-D14-3 | Setting `services.tally-uplink.kit` and `.plan` — the box's argv table and the acceptor's plan body | Both stay null and null is the honest state, not an oversight. NO kit names an argv for this estate: TL-18 is the open Tom line on the Claude-seat one ("no kit names a Claude argv today"), and the uplink never falls back to a command of its own — a proposal carrying an `argv_ref` it cannot resolve is a legible throw, not a guess. The plan body is the ACCEPTOR's, re-POSTed to arm and re-arm; authoring one in this module would be the lake proposing from the wrong side of the seam | TL-18 for the kit (`kits/claude/UNIT.md`, Tom's line or paste), then a follow-up edit of these two options — reviewed like any other change, which the `tally-uplink-topology` check sees because it asserts both are null today |
| DF-U-D14-4 | Giving the uplink a schedule: a timer, an `Install` section, or any `WantedBy` that would fire it | The uplink holds no schedule of its own, by its own card's non-goal ("no scheduling logic in the uplink: the lake proposes, the door answers") and by spec §2.2b — "the uplink wakes on a socket event, on `next_wake_at`, or on a verdict it posted — whichever first — and every wake is a probe plus a pull; there is no interval". `wakes = 1` here is one wake per invocation, never a loop this file authorises, and the rendered unit carries no `Install` section at all (asserted at eval time and by clause E) | U-D18 (DF-FILLER-TIMER): "a systemd user timer that runs the filler lane … the timer only wakes the uplink's filler pass, the kernel admits". Discharged when that unit lands its own timer; nothing in U-D14 shortens the wait |
| DF-U-D18-1 | Switching the coordinator generation, so `tally-filler.timer` is actually installed and armed by `timers.target` — i.e. `sudo nixos-rebuild switch --flake /home/tom/mecattaf/dotfiles#coordinator`, and anything hand-started in its place | U-D18 switches nothing; `~/research-methods/DECISIONS.md` D-B15 assigns the coordinator switch to U-D19 and keeps the worker box's a TOM LINE. A declaration is the deliverable, never a hand-installed unit (Rule 9) — which is exactly why the oracle's run proof is a TRANSIENT `systemd-run --user` unit under a different name (`tally-filler-probe`) that it stops on exit. The card's own post-switch clause is U-D19's post-condition by the orchestrator's D-B66 note, since U-D19 dependsOn U-D18 and inverting that graph is not this unit's to do. MEASURED 2026-09-07 on the coordinator: `systemctl --user show tally-filler.timer --property=LoadState` → `not-found`, the declared-but-not-switched state, which clause F records as a NOTE rather than a verdict | U-D19 switches the coordinator in the P05 order. Discharged when `systemctl --user list-timers` names `tally-filler.timer` with a `FragmentPath` under `/nix/store` and `bash tools/u-d18-filler-timer-oracle.sh` is green in the repo — then delete this row |
| DF-U-D18-2 | Setting an anti-starvation number for the filler lane, and implementing D-B10's age-based promotion (a filler item passed over by 10 non-filler releases proposed at level 1 for one release) | Spec §4.4.3 names TL-10 as the anti-starvation number and says that "until it exists the lane runs whenever the row is free". The promotion is a RELEASE-STATION decision over a backlog — the acceptor's `levels` document and the lake's proposals — and a clock has no backlog to age. Putting a promotion rule in a timer would be this unit deciding admission, which is the one thing its own card forbids ("the timer only wakes the uplink's filler pass, the kernel admits") | Tom for TL-10's number; the release station (§2.2c) and the lake's selector for the promotion itself. Nothing in this repository shortens the wait, and the five-minute cadence stands meanwhile: it is the drain's own, so neither filler can crowd the other out |
| DF-U-D18-3 | Moving the academic drain onto a LEASE over the kernel's socket, so the two fillers alternate UNDER the kernel's admission rather than beside it | Spec §2.4 lists the drain as an *unleased* tenant of the GPU row "until TL-15 moves the drain onto a lease over the socket (U-D11 wires the call)". Until that happens, D-B10's round-robin is carried here by equal cadence plus the lane's own `/running`-empty gate — two mechanisms outside the kernel — and this unit cannot lease on another unit's behalf. MEASURED 2026-09-07: `tally-drain.service` runs `tally --socket /run/user/1000/tally/tally.sock daemon drain`, the LIVE daemon's socket, not the rewrite kernel's `~/.local/state/tally-rewrite/kernel.sock` | U-D11 wires the call; TL-15 is the line. Discharged when the drain's GPU turn is a lease on the chain and the `tally-filler-topology` cadence equality can be replaced by an admission-side assertion |

**How DF-U-D16-1 is discharged.** `bash tools/u-d16-l8-flash-oracle.sh` prints
its state as a `NOTE` row (`5e`), and
`home/dot_local/bin/l8-flash-probe` prints it as `[E] hand-written pair gone`.
When both read green on the coordinator, delete the row. DF-U-D17-1 is the same
shape one lane later: `l8-flash-probe` prints it as the two
`[E] util-*.timer declared` rows, and `bash tools/u-d17-util-01-oracle.sh`
gates the repository half around them. A row leaves when the switch has been
taken and the rows read green on the box, not when the merge landed.
The U-D12 rows likewise leave only when the actor and condition in their last
column have actually occurred; a fresh UNKNOWN observation does not discharge
its underlying deferral. DF-U-D15-1 is the same shape again: the input is
re-pinned and green in the repo, and stays undischarged until the store path
`tests/herdr/test-herdr-kitten-input.sh` prints is the one `hk` on each box
actually resolves to.

DF-U-D18-1 is DF-U-D13-1's and DF-U-D14-1's twin a third time, and discharges
in the same act: one switch installs the kernel on the system bus and, on tom's,
the uplink and the filler's clock. DF-U-D18-2 and DF-U-D18-3 are of the other
kind — they are not waiting on a switch at all, but on a number Tom has not
named (TL-10) and on a call another unit wires (U-D11/TL-15) — so neither will
be discharged by anything U-D19 does, and both stay data in the meantime.

DF-U-D14-1 is DF-U-D13-1's twin one bus over and discharges in the same act:
the coordinator switch installs the kernel on the system bus and the uplink on
tom's, and neither row leaves until the box says so. DF-U-D14-2 is the only row
in this table whose actor cannot be an executor at all — a credential is Tom's
by construction, and the unit is deliberately built to fail loudly rather than
run without one.
