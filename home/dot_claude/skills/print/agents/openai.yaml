interface:
  display_name: "Print Paper"
  short_description: "Drop Markdown into ~/Paper/intake; the paper daemon prints it"
  default_prompt: "Use $print to drop this Markdown into ~/Paper/intake so the paper daemon prints it."
