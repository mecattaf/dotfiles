# home/private_dot_env.tmpl
# Generated by Chezmoi - DO NOT EDIT MANUALLY

# System Information (from .chezmoi.yaml)
HOSTNAME="{{ .chezmoi.hostname }}"
USER="{{ .user.name }}"

# API Keys (from encrypted secrets)
GITHUB_TOKEN="{{ .api_keys.github }}"
OPENAI_API_KEY="{{ .api_keys.openai }}"
ANTHROPIC_API_KEY="{{ .api_keys.anthropic }}"

# Database (from encrypted secrets)
DB_PASSWORD="{{ .database.password }}"
DATABASE_URL="{{ .database.connection_string }}"

# AWS Credentials (from encrypted secrets)
AWS_ACCESS_KEY_ID="{{ .aws.access_key }}"
AWS_SECRET_ACCESS_KEY="{{ .aws.secret_key }}"

# Application Config (from non-encrypted data)
NODE_ENV="production"
PORT=3000
DEBUG=false
