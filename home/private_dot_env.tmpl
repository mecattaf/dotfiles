# ═══════════════════════════════════════════════════════════════════════════
# 🔐 SECRET ENVIRONMENT VARIABLES
# Generated by Chezmoi - DO NOT EDIT MANUALLY
# Source this file: source ~/.env
# ═══════════════════════════════════════════════════════════════════════════

# ═════════════════════════════════════════════════════════════════════════
# 🌐 GITHUB AUTHENTICATION
# ═════════════════════════════════════════════════════════════════════════
export GH_TOKEN="{{ .secrets.github.cli_pat }}"

# ═════════════════════════════════════════════════════════════════════════
# 🤖 LLM API KEYS (for CLI tools)
# ═════════════════════════════════════════════════════════════════════════
export OPENAI_API_KEY="{{ .secrets.llm_providers.openai }}"
export ANTHROPIC_API_KEY="{{ .secrets.llm_providers.anthropic }}"
export GEMINI_API_KEY="{{ .secrets.llm_providers.gemini }}"
export GROQ_API_KEY="{{ .secrets.llm_providers.groq }}"

# ═════════════════════════════════════════════════════════════════════════
# 🌐 TAILSCALE OAUTH (for automation scripts)
# ═════════════════════════════════════════════════════════════════════════
export TAILSCALE_OAUTH_CLIENT_ID="{{ .secrets.tailscale.oauth_client_id }}"
export TAILSCALE_OAUTH_CLIENT_SECRET="{{ .secrets.tailscale.oauth_client_secret }}"

# ═════════════════════════════════════════════════════════════════════════
# 🔑 INTERNAL SERVICE KEYS
# ═════════════════════════════════════════════════════════════════════════
export LITELLM_MASTER_KEY="{{ .secrets.api_keys.litellm_master }}"
export OPENWEBUI_SECRET_KEY="{{ .secrets.api_keys.openwebui_secret }}"

# ═════════════════════════════════════════════════════════════════════════
# 📝 SYSTEM INFORMATION
# ═════════════════════════════════════════════════════════════════════════
export HOSTNAME="{{ .chezmoi.hostname }}"
export USER="{{ .user.name }}"
