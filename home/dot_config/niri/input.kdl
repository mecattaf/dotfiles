// Managed as a raw Home Manager out-of-store symlink so niri can hot-reload it.

input {
    keyboard {
        // scroll: input type:keyboard { xkb_options caps:none }
        xkb {
            options "caps:none"
        }
        numlock
    }

    touchpad {
        // scroll: input type:touchpad { tap enabled }
        tap
        // scroll: input type:touchpad { dwt enabled }
        dwt
        // scroll: input type:touchpad { natural_scroll enabled }
        natural-scroll
        accel-profile "adaptive"
        // scroll: input type:touchpad { click_method clickfinger }
        click-method "clickfinger"
    }

    mouse {
        accel-profile "flat"
    }

    trackpoint {}

    // NO touch mapping here — this file is deployed FLEET-WIDE and must parse on
    // stock niri, which is now what every host runs. Per-device touch → output
    // blocks (`touch "<name>" { map-to-output }`, PR #1856 syntax) belong in the
    // host-gated ~/.config/niri-local.kdl (generated by home.nix, included from
    // config.kdl) and need a niri that understands that syntax. The only host
    // that ever did was the zenbook-duo, retired 2026-08-30 along with the
    // niri-pr1856 overlay attr it was the sole builder of.

    // scroll: focus_follows_mouse no
    // ^ niri defaults to click-to-focus. Using focus-follows-mouse with scroll prevention:
    // focus-follows-mouse max-scroll-amount="0%"

    // scroll: gesture_scroll_enable true
    // scroll: gesture_scroll_fingers 4
    // scroll: gesture_scroll_sensitivity 1.0
    // ^ NO NIRI EQUIVALENT

    // scroll: focus_wrapping no
    // ^ NO NIRI EQUIVALENT (niri's scrolling model doesn't wrap)
}

clipboard {
    disable-primary
}
