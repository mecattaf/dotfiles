# ═══════════════════════════════════════════════════════════════════════════
# 🔐 SECRET ENVIRONMENT VARIABLES
# Generated by Chezmoi - DO NOT EDIT MANUALLY
# ═══════════════════════════════════════════════════════════════════════════

# GitHub CLI authentication
set -gx GH_TOKEN "{{ .secrets.github.cli_pat }}"

# LLM API Keys (if needed in shell)
set -gx OPENAI_API_KEY "{{ .secrets.llm_providers.openai }}"
set -gx ANTHROPIC_API_KEY "{{ .secrets.llm_providers.anthropic }}"

# Tailscale OAuth (if needed for scripts)
set -gx TAILSCALE_OAUTH_CLIENT_ID "{{ .secrets.tailscale.oauth_client_id }}"
set -gx TAILSCALE_OAUTH_CLIENT_SECRET "{{ .secrets.tailscale.oauth_client_secret }}"
