#!/bin/bash
# launchpad -- toggle the overlay surface (launchpad merged into overlay)
# IPC target: ShellBridge (target: "shell"), method: toggleOverlay
qs ipc --pid $(pgrep -x qs) call shell toggleOverlay
