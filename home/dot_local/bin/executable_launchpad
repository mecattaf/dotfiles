#!/bin/bash
# CHANGED: launchpad IPC target renamed to shell toggleOverlay
# Old: quickshell ipc call launchpad toggle
# New: quickshell ipc call shell toggleOverlay
quickshell ipc call shell toggleOverlay
