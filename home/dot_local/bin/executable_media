#!/bin/bash
# media -- MPRIS playback control via IPC
# IPC target: music (MprisBridge IpcHandler)
case "$1" in
    play|toggle)  qs ipc --pid $(pgrep -f "qs.*webshell") call music togglePlay ;;
    next)         qs ipc --pid $(pgrep -f "qs.*webshell") call music next ;;
    prev)         qs ipc --pid $(pgrep -f "qs.*webshell") call music previous ;;
    *)            echo "Usage: $0 {play|next|prev}"; exit 1 ;;
esac
