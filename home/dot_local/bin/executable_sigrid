#!/bin/bash
# sigrid -- toggle the CCD sidebar (AI/sigrid panel is inside CCD)
# IPC target: ShellBridge (target: "shell"), method: toggleCcd
qs ipc --pid $(pgrep -f "qs.*webshell") call shell toggleCcd
