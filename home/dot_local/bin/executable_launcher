#!/bin/bash
# launcher -- toggle the overlay (launcher/spotlight) surface
# IPC target: ShellBridge (target: "shell"), method: toggleOverlay
qs ipc --pid $(pgrep -f "qs.*webshell") call shell toggleOverlay
