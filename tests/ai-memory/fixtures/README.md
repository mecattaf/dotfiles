# Synthetic AI-memory traces

Every JSONL file below is a hand-authored, sanitized protocol fixture. The
identities, paths, prose, tool calls, and marker bodies are fictional. No line
was copied from a real Claude Code or Codex session.

The fixtures intentionally contain obvious `SECRET_*` sentinels inside fields
that the normalizer must discard.
