# Track substrate fills this (DESIGN.md 12.1 phases 2 and 3, Substrate
# subtests): every Substrate workload Available, atelet Ready on the
# coordinator only, the WorkerPool Ready, gVisor through the RustFS fallback.
# Left empty by the cluster track on purpose; it runs after 10-cluster.
